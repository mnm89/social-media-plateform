const path = require("path");

require("dotenv").config(); // Load .env variables

module.exports = {
  config: path.resolve("src/config/database.js"), // Path to your database config
  "models-path": path.resolve("src/models"), // Path to models folder
  "migrations-path": path.resolve("src/migrations"), // Path to migrations folder
  "seeders-path": path.resolve("src/seeders"), // Path to seeders folder (optional)
};
